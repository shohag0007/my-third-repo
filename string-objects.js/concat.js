// gap between full name.
const first = 'Delowar';
const last = 'Hossain';
// const fullName = first + ' ' + last;
// console.log(fullName);

// another way

const fullName = first.concat(' ').concat(last);
console.log(fullName);

// includes

console.log(last.includes('f'));